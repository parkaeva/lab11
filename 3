#include <iostream>
using namespace std;
int main()
{
    int n, k, sum, a;
    cin>>n;
    sum=0;
    k=0;
    while(sum<n)
    {
       k+=1; 
       sum=sum+k;
    }
    cout<<k<<endl<<sum;
    return 0;
}
